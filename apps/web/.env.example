# Supabase — the ANON key here is safe to expose to the browser (RLS enforces access).
NEXT_PUBLIC_SUPABASE_URL=https://ckxxpyzxsbhhynlboyid.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Base URL of the Express API (onboarding, orders). Used later.
NEXT_PUBLIC_API_URL=http://localhost:4000
